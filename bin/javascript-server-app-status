#!/usr/bin/env bash

source ~/datastore/datastore-support/env/datastore-env

LOCKFILE=~/datastore/datastore-deployment/var/lock/javascript-server-app.pid

if [ ! -f "$LOCKFILE" ]; then
    echo "$LOCKFILE doesn't exist, service doesn't appear to be running"
    exit 1
fi

ps -p `cat $LOCKFILE` -o pid,vsz=MEMORY -o comm,args=ARGS
